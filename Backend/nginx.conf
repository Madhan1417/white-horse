events {}

http {
    server {
        listen 4000;

        root /usr/share/nginx/html;
        index index.html;

        location /healthz {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
